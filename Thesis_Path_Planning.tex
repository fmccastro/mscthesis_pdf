%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                                                                      %
%     File: Thesis_Path_Planning.tex                                   %
%     Tex Master: Thesis.tex                                           %
%                                                                      %
%     Author: Andre C. Marta                                           %
%     Last modified : 13 May 2019                                      %
%                                                                      %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Global, optimal, terrain aware path planner}
\label{chapter:globalpathplanner}

In order to deliver optimal reference paths, an offline survey of the target environment is performed at first. In section \ref{section:costMap}, an analysis of the terrain uneveness is carried out by taking into account the vehicle geometric features. After retrieving a \tb{traversability cost map} from the previous section, a modified version of it is fed to the Eikonal equation in section \ref{section:optimalPathPlanning}, in order to yield a gradient potential map, from where an optimal path to some goal position $G$ is generated. All the results presented in this chapter were obtained on an Intel® Xeon® E5-1620 v2 × 8 processor.

\section{Traversability map computation}
\label{section:costMap}

As it is mentioned in subsection \ref{subsection:worldModelling}, there is no explicit guideline on how to generate a traversability map. In this thesis, several approaches are compared. First, the state-of-the art algorithm from \citet{Amorim2014} is reviewed and evaluated. Then, this algorithm is reformulated to take into account other metrics such as terrain inclination and roughness \cite{Ishigami2007PathPlanning}. Finally, these two maps are merged to yield a conservative, worst-case version of them, followed by its refinement, as it is shown in figure \ref{fig:travmapsgenerationflowchart}.

\begin{figure}[h]
\centering
\begin{tikzpicture}[sibling distance=40mm, level distance=12mm]
  \node {\hyperref[subsubsec:costMapRefinement]{Refined map}} [grow'=up]
    child[<-] {node {\hyperref[subsection:worstCaseScenario]{Worst-case map}}
      child[<-] {node {\hyperref[subsection:maxTerrainRoughness]{Maximum terrain roughness}}}
      child[<-] {node {\hyperref[subsection:heightMinimization]{Height minimization}}}
      };
\end{tikzpicture}
\caption[Traversability maps generation flowchart.]{Traversability maps generation flowchart with respective dependencies.}
\label{fig:travmapsgenerationflowchart}
\end{figure}

\subsection{Height minimization}
\label{subsection:heightMinimization}

A similar constrained optimization problem to the one from \cite{Amorim2014}, which minimizes the height of the robot at each cell is described here. In this case, a wheeled vehicle is used instead of a tracked one. The yaw angle is set as a fixed variable and the initial guess $\boldsymbol{w}_0$ of the problem at every cell is set by computing an \ti{a-priori} approximation to the optimal solution.

\subsubsection*{Initial guess}

Provided that the yaw angle $\yaw$ and the $\x$ and $\y$ position coordinates are known, each wheel point is projected to $\Map$. Then, from the group of retrieved contact points, the lowest height one is discarded, and a normalized, ortogonal vector to a plane which contains these points, is computed. Finally, the roll and pitch of this plane are given by \refeq{eq:rollPitchInitialGuess}, where $\mathbf{\hat{n}}$ denotes the normalized ortogonal vector to the above mentioned plane. In case any of these angles are higher than $\roll_{max}$ or $\pitch_{max}$, the respective angle is set to the highest admissable value, so that the initial guess is feasible. The elevation initial guess $\z_0$ is set as the sum of the projection of the robot center of mass point to $\Map$ plus the robot \acrshort{com} geometric height, to avoid any possible intrusion of the vehicle hull into the ground.

\begin{equation}
	\begin{aligned}
		\roll &= \arcsin \left( \sin(\yaw) \, \hat{n}_x -\cos(\yaw)\, \hat{n}_y \right) \\
		\pitch &= \arccos \left( \frac{\hat{n}_z}{\cos\left( \roll \right)} \right) \\
	\end{aligned}	
	\label{eq:rollPitchInitialGuess}
\end{equation}

\subsubsection*{Nonlinear programming}

The minimization of the vehicle's center of mass is expressed as a nonlinear optimization problem (NLP) in \refeq{eq:minHeight}, where $\boldsymbol{\Orientation}_{max}$ stands as the limits for each euler angle. $\mathbf{p}_{3,\,k}$ stands as the $k^{th}$ hull point position, which is defined with respect to $\WorldFrame$, by taking into account the robot geometry. $[\x, \y, \yaw]^T$ are fixed variables whereas $\left[ {\z},\, {\roll},\, {\pitch}\right]^T$ are set as optimization variables. Each hull point included in \refeq{eq:minHeight} adds an additional constraint by stating each hull point must be above or at contact with the terrain. In case less than three hull points (each one from a different wheel) are at contact with the terrain, the terrain inclination returned by algorithm \nameref{alg:slopeMinHeight} is $\frac{\pi}{2}$. This expresses the fact that the robot will stand at an unstable position, so this position is regarded as an undesirable traversable position.  On the other hand, the terrain inclination is given by \refeq{eq:inclination}, which returns the angle between a normalized vector with direction $\hat{\BodyFrame}_z$ and the normalized vector $\hat{\WorldFrame}_z$ \cite{Amorim2014}. Although algorithm \nameref{alg:slopeMinHeight} can be extended to any kind of wheeled-mobile robot configuration, only four wheel mobile robots are considered in this thesis, as it is mentioned in section \ref{section:problemFormulation}.

\raisebox{2ex}
{
	\begin{minipage}[c]{0.35\linewidth}
    \centering
    \begin{eqnarray}
  		{\rm Minimize}   && J = \z \nonumber \\
  		{\rm w.r.t.}     && \boldsymbol{w} = \left( {\z},\, {\roll},\, {\pitch} \right) \nonumber \\
  		{\rm subject~to} && \forall_k: \z_k - \FunctionElevation(\x_k, \y_k) \geq 0 \hspace{10mm} \label{eq:minHeight} \\
                   	 	 && \boldsymbol{\Orientation}_{max} - \left| \boldsymbol{\Orientation} \right| \geq 0 \, \nonumber
	\end{eqnarray}
	\begin{equation}
		\TraversabilityCost = \arccos( \cos(\pitch) \cos(\roll) )
		\label{eq:inclination}
	\end{equation}
  	\end{minipage}
}
\hfill
\raisebox{2ex}
{
	\begin{minipage}[c]{0.50\linewidth}
    \begin{algorithm}[H]
	\caption{\footnotesize \textsc{CELLSLOPEFIXYAW}}\label{alg:slopeMinHeight}
	\centering
	\footnotesize
	\begin{algorithmic}[1]
	\Function{terrainslope}{$\boldsymbol{\Orientation}_{max},\, \FunctionElevation(\cdot),\, \boldsymbol{w}_0,\, x,\, y, \, \yaw$}
	\State $\boldsymbol{w}^* \gets \textsc{heightmin}(\boldsymbol{w}_0, \, \boldsymbol{\Orientation}_{max},\, \FunctionElevation(\cdot), \,x, \,y, \, \yaw)$ \Comment{\eqref{eq:minHeight}}
	\If{$\textmd{Contacts} \geq 3$}
	\State $\TraversabilityCost \gets \textsc{inclination}(\roll^*,\, \pitch^*)$ \Comment{\eqref{eq:inclination}}
	\Else
	\State $\TraversabilityCost \gets \frac{\pi}{2}$
	\EndIf
	\State \Return $\TraversabilityCost, \, \boldsymbol{w}^*$
	\EndFunction
	\end{algorithmic}
	\end{algorithm}
	\end{minipage}
}

\begin{algorithm}[H]
	\caption{\footnotesize \textsc{MAPSLOPEFIXYAW}}\label{alg:slopeMinHeightMap}
	\centering
	\footnotesize
	\begin{algorithmic}[1]
	\Function{inclinationfixyaw}{$\boldsymbol{\Orientation}_{max}, \, \yaw, \, \FunctionElevation(\cdot), \,n_x,\, n_y$}
	\ForEach{$\{i,\,j\} \in \{0,\, \ldots, \, n_x - 1\} \times \{0,\, \ldots, \, n_y - 1\}$}
	\State $\boldsymbol{w}_0 \gets \textsc{initialguess}(x_i,\, y_j, \, \yaw)$ \Comment{feasible initialization}
	\If{$\Robot_{\mathbf{p}_2} \subset \Map_2$}
	\State $\TraversabilityCost,\,\boldsymbol{w}^* \gets \textsc{terrainslope}(\boldsymbol{\Orientation}_{max},\, \FunctionElevation(\cdot), \, \boldsymbol{w}_0, \, x_i, \, y_j, \, \yaw)$ \Comment{algorithm \nameref{alg:slopeMinHeight}}
	\Else
	\State $c \gets \frac{\pi}{2}$
	\EndIf
	\State $\FunctionTraversability_d(i,\, j) \gets c$
	\EndFor
	\State \Return $\FunctionTraversability_d(\cdot)$ 
	\EndFunction
	\end{algorithmic}
\end{algorithm}

Then, algorithm \nameref{alg:slopeMinHeightMap} extends the application of algorithm \nameref{alg:slopeMinHeight} to every cell of the elevation grid. Moreover, a convex area $\Robot_{\mathbf{p}_2}$ (\refeq{eq:robotHullArea}) centered at $\mathbf{p}_2$ (typically the robot's \acrshort{com}), must capture the robot's geometric hull $\mathcal{H}_2$ projection on $\Map$ and lie inside the map domain. If this condition is not met, the terrain inclination angle assigned to $\FunctionTraversability_d(\cdot)$ is $\frac{\pi}{2}$. Therefore, positions at which the assigned terrain inclination is $\frac{\pi}{2}$ are considered to be untraversable. To sum up, the higher the inclination, the less desirable it is to pass through the respective position.

\begin{figure}[!htb]
  \begin{minipage}[c]{0.35\linewidth}
    \centering
    \begin{tikzpicture}
		\draw[->] (0.0, 0.0) -- (1.0, 0.0) node[pos=1.3] {$\WorldFrame_x$};
		\draw[->] (0.0, 0.0) -- (0.0, 1.0) node[pos=1.3] {$\WorldFrame_y$};
		\fill[gray, thick, rotate around={30:(3.5,1.25)}] (2.0, 0.5) rectangle (5.0, 2.0);
		\filldraw[black] (3.5, 1.25) circle (2pt) node[anchor=north]{$\mathbf{p}_2$};
		\draw[color=black, thick] (3.5, 1.25) circle (1.677);
		\draw[->] (3.5, 1.25) -- (4.366, 1.75) node[pos=1.3] {$\BodyFrame_x$};
		\draw[->] (3.5, 1.25) -- (3.0, 2.116) node[pos=1.3] {$\BodyFrame_y$};
		\node at (3.5,0.5) [below] {$b$};
		\node at (2.26, 0.6) [left] {$l$};
		\node at (1.9, 1.25) [left] {$\Robot_{\mathbf{p}_2}$};
		\node at (2.8, 1.8) [left] {$\RobotHullArea_2$};
	\end{tikzpicture}
  \end{minipage}
  \begin{minipage}[c]{0.6\linewidth}
    \centering
	\begin{equation}
		\Robot_{\mathbf{p}_2} = \{ \text{min} \, \underline{h}: \| \mathbf{\Position}_2 - \mathbf{a} \| \leq \underline{h}\;\text{such that}\;\mathbf{p}_{2, k},\,\mathbf{a} \in \RobotCircleHullArea_2 \}
		\label{eq:robotHullArea}
	\end{equation}
  \end{minipage}
\caption{Robot hull area.}
\label{fig:robotHull}
\end{figure}

\begin{remark}
The discrete elevation map $\FunctionElevation_d(\cdot)$ shall have such dimensions that the cell divison length is smaller than the order of magnitude of the wheel diameter.
\end{remark}

\subsubsection*{Variable yaw}

The terrain traversability may also be assessed by finding the maximum inclination at each position by saving the maximum retrieved inclination among several yaws. Algorithm \nameref{alg:slopeMinHeightMap} lacks a thorough evaluation of the terrain irregularity, as this feature changes depending on the mobile robot heading. In order to tackle this absence of completeness, algorithm \nameref{alg:slopeMinHeightMap} is reformulated into algorithm \nameref{alg:slopeMinHeightMapVarYaw} to address this issue.

\begin{algorithm}[H]
	\caption{\footnotesize \textsc{MAPSLOPE}}\label{alg:slopeMinHeightMapVarYaw}
	\centering
	\footnotesize
	\begin{algorithmic}[1]
	\Function{inclinationvaryaw}{$\boldsymbol{\Orientation}_{max}, \, \FunctionElevation(\cdot), \,n_x,\, n_y$}
	\ForEach{$\{i,\,j\} \in \{0,\, \ldots, \, n_x - 1\} \times \{0,\, \ldots, \, n_y - 1\}$}
	\ForEach{$\yaw_k \in \mathcal{Y}$} \Comment{change heading}
	\State $\boldsymbol{w}_0 \gets \textsc{initialguess}(x_i, \, y_j, \, \yaw_k)$ \Comment{feasible initialization}
	\If{$\Robot_{\mathbf{p}_2} \subset \Map_2$}
	\State $\TraversabilityCost_k,\, \left( \boldsymbol{w}^* \right)_k \gets \textsc{terrainslope}(\boldsymbol{\Orientation}_{max}, \, \FunctionElevation(\cdot), \, \boldsymbol{w}_0, \, x_i, \, y_j, \, \yaw_k)$ \Comment{algorithm \nameref{alg:slopeMinHeight}}
	\Else
	\State $\TraversabilityCost_k \gets \frac{\pi}{2}$
	\EndIf
	\EndFor
	\State $\TraversabilityCost \gets \text{max}\left( \TraversabilityCost_k \right)$ \Comment{save maximum inclination}
	\State $\FunctionTraversability_d(i,\, j) \gets \TraversabilityCost$ \Comment{assign $c$ to respective cell in discrete traversability map}
	\EndFor
	\State \Return $\FunctionTraversability_d(\cdot)$
	\EndFunction
	\end{algorithmic}
\end{algorithm}

In algorithm \nameref{alg:slopeMinHeightMapVarYaw}, $\mathcal{Y} = \lbrace -\pi,\, \ldots,\, -\pi + k \alpha, \, \ldots, \, \pi - \alpha \rbrace$, where $\alpha$ stands as the step angle between consecutive elements of $\mathcal{Y}$.

\subsection{Maximum terrain roughness}
\label{subsection:maxTerrainRoughness}

Terrain traversability may also be evaluated by inspecting terrain roughness around each position of the map, where the goal is to evaluate the maximum terrain uneveness at each position of the map. In this thesis, terrain roughness at each position of $\Map$ is defined as the standard deviation of elevations inside $\RobotHullArea_2$.

\begin{equation}
	\bar{\FunctionElevation}_d(i, j) = \frac{\sum_{\x_i,\,\y_j \in \RobotHullArea_2} \FunctionElevation(x_i, \, y_j)}{n(\underline{\RobotHullArea}_2)},\; \underline{\RobotHullArea}_2 :=  \forall \left( \x_i,\, \y_j \right) \in \RobotHullArea_2
	\label{eq:averageHeight}
\end{equation}

\noindent, where $\underline{\RobotHullArea}_2$ denotes a finite set of points from $\FunctionElevation_d(\cdot)$ inside $\RobotHullArea_2$ and $\bar{\FunctionElevation}_d(i, j)$ stands as the average of elevations of points inside $\RobotHullArea_2$. Then, terrain roughness is defined as,

\begin{equation}
	\FunctionTraversability_d(i,\,j) = \sqrt{\frac{\sum_{\x_i,\,\y_j \in \RobotHullArea_2} \left( \FunctionElevation_d(i,\,j) - \bar{\FunctionElevation}_d(i,\,j) \right)^2 }{n(\underline{\RobotHullArea}_2)}}
	\label{eq:terrainRoughness}
\end{equation}

The set of points inside $\underline{\RobotHullArea}_2$ depends on the yaw angle of the mobile robot, so $\FunctionTraversability_d(i,\,j)$ is computed with respect to a given heading. Similarly to what was done in algorithm \nameref{alg:slopeMinHeightMapVarYaw}, the terrain roughness assigned to the respective cell is the maximum roughness among the set of yaws $\mathcal{Y}$. Finally, a maximum terrain roughness may be computed by implementing algorithm \nameref{alg:maxRoughnessMap}.

\begin{algorithm}[H]
	\caption{\footnotesize \textsc{MAPROUGHNESS}}\label{alg:maxRoughnessMap}
	\centering
	\footnotesize
	\begin{algorithmic}[1]
	\Function{roughness}{$\boldsymbol{\Orientation}_{max}, \, \FunctionElevation(\cdot), \,n_x,\, n_y$}
	\ForEach{$\{i,\,j\} \in \{0,\, \ldots, \, n_x - 1\} \times \{0,\, \ldots, \, n_y - 1\}$}
	\If{$\Robot_{\mathbf{p}_2} \subset \Map_2$}
	\ForEach{$\yaw_k \in \mathcal{Y}$} \Comment{change heading}
	\State $\TraversabilityCost_k \gets \text{equation \eqref{eq:terrainRoughness}}$
	\EndFor
	\Else
	\State $\TraversabilityCost_k \gets \texttt{None}$
	\EndIf
	\State $\TraversabilityCost \gets \text{max}\left( \TraversabilityCost_k \right)$ \Comment{save maximum roughness}
	\State $\FunctionTraversability_d(i,\, j) \gets \TraversabilityCost$
	\EndFor
	\State \text{Replace cells assigned with $\texttt{None}$ by max} $\left( \FunctionTraversability_d(\cdot) \right)$
	\State \Return $\FunctionTraversability_d(\cdot)$ 
	\EndFunction
	\end{algorithmic}
\end{algorithm}

\begin{remark}
In case $\RobotCircleHullArea_2 \not\subset \Map_2$, the respective cell is assigned with $\texttt{None}$. At the end, these assignments are replaced by the maximum roughness among all cells, in order to enhance the fact that these cells are meant to be among the most untraversable ones.
\end{remark}

\subsection{Worst-case scenario}
\label{subsection:worstCaseScenario}

A more conservative traversability map can be created by assigning to every cell, the highest value among the traversability maps computed by algorithms \nameref{alg:slopeMinHeightMapVarYaw} and \nameref{alg:maxRoughnessMap} at the respective cell. This approach provides a \tb{joint awareness} of terrain inclination and roughness on a single traversability map. In order to accomplish that, the traversability maps that come into play are normalized in order to enable a fair comparison among maps.

\subsubsection*{Traversability map refinement}
\label{subsubsec:costMapRefinement}

In order to classify high cost borderline areas as undesirable, a refinement of the worst-case scenario map is made, so that a mobile robot avoids a close traverse to undesirable positions. Algorithm \nameref{alg:refinedMap} presents a way to code this refinement method, by increasing the cost of areas around positions with a cost higher than a defined threshold, $c_{max}$. At each point with cost higher than $c_{max}$, a \tb{decreasing, isometrical source} radiates up to a distance $r_s$ from the respective point. To every point that is inside the source, the existing cost is replaced by the source cost $\TraversabilityCost_s \left(\cdot \right)$ in case this one is higher.

\begin{equation}
\TraversabilityCost_s \left(x \right) = \begin{cases} -\left( \frac{x}{r_s} \right)^2 + 1 \, , \hspace{5mm} 0 \leq x \leq r_s\\
0,\,x > r_s \end{cases}
\label{eq:sourceCost}
\end{equation}

\begin{algorithm}[H]
	\caption{\footnotesize \textsc{REFINEWORSTCASE}}
	\label{alg:refinedMap}
	\centering
	\footnotesize
	\begin{algorithmic}[1]
	\Require $\boldsymbol{\Orientation}_{max}, \, \FunctionElevation(\cdot), \,n_x,\, n_y, \, r_s, \, C$
	\State $\FunctionTraversability_{d, \, inc}(\cdot) \gets \textsc{inclinationvaryaw}(\boldsymbol{\Orientation}_{max}, \, \FunctionElevation(\cdot), \,n_x,\, n_y)$ \Comment{run algorithm \nameref{alg:slopeMinHeightMapVarYaw}} 
	\State $\FunctionTraversability_{d,\,r}(\cdot) \gets \textsc{roughness}(\boldsymbol{\Orientation}_{max}, \, \FunctionElevation(\cdot), \,n_x,\, n_y)$ \Comment{run algorithm \nameref{alg:maxRoughnessMap}}
	\State $\FunctionTraversability_{d, \, inc}(\cdot),\, \FunctionTraversability_{d,\,r}(\cdot) \gets \frac{\FunctionTraversability_{d, \, inc}(\cdot)}{\text{max}\left( \FunctionTraversability_{d, \, inc}(\cdot) \right)}, \, \frac{\FunctionTraversability_{d,\,r}(\cdot)}{\text{max} \left( \FunctionTraversability_{d,\,r}(\cdot) \right)}$ \Comment{normalize maps}
	\ForEach{$\{i,\,j\} \in \{0,\, \ldots, \, n_x - 1\} \times \{0,\, \ldots, \, n_y - 1\}$}
	\State $\FunctionTraversability_d(i,\, j) \gets \text{max} \left(\FunctionTraversability_{d, \, inc}(i,\,j), \hspace{1mm} \FunctionTraversability_{d,\,r}(i,\,j) \right)$ \Comment{generate worst-case map}
	\EndFor
	\ForEach{$\{i,\,j\} \in \{0,\, \ldots, \, n_x - 1\} \times \{0,\, \ldots, \, n_y - 1\}$}
	\If{$\FunctionTraversability_d(i,\, j) \geq C$}
	\State \text{Add $\left( i, \, j \right)$ to $p$} \Comment{add point with cost above or equal to $C$ to list}
	\EndIf
	\EndFor
	\ForEach{$\{i,\,j\} \in p$}
	\ForEach{$\{x_{i_2},\, y_{j_2}\} \in \{x_i - r_s,\,\ldots,x_i + r_s\} \times \{y_j - r_s,\,\ldots, y_j + r_s\}$}
	\If{$\{x_{i_2},\,y_{j_2}\} \in \Map_2$}
	\State $d \gets \sqrt{\left( x_{i_2} - x_i \right)^2 + \left( y_{j_2} - y_j \right)^2}$ \Comment{compute distance between $\left(i,\,j\right)$ and $\left(i_2,\,j_2\right)$}
	\State $T_d\left(i_2,\, j_2 \right) \gets c_s(d)$ \Comment{add \refeq{eq:sourceCost} result to list of source costs which concern $\left(i_2,\, j_2 \right)$}
	\EndIf
	\EndFor
	\EndFor
	\ForEach{$\{i,\,j\} \in \{0,\, \ldots, \, n_x - 1\} \times \{0,\, \ldots, \, n_y - 1\}$}
	\State $\FunctionTraversability_d\left(i,\, j\right) \gets \text{max}\left( \FunctionTraversability_d(i,\, j),\,T_d\left(i,\, j\right) \right)$ \Comment{assign highest cost among source costs and existing cost}
	\EndFor
	\end{algorithmic}
\end{algorithm}

\subsection{Implementation}
\label{subsection:costMapImplementation}

In this subsection, the implementation of the algorithms presented above is described, alongside the presentation of results. The elevation map used in this implementation contains regions with a variety of terrain, that range from irregular to smooth areas, but also from hilly to planar locations. In figure \ref{fig:elevationmapdimensions}, the elevation map is rendered. The wheeled mobile robot Pioneer 3AT was chosen as the test vehicle. The mobile robot dimensions are presented in table \ref{tab:robotDimensions}, whereas the elevation map features are presented in the table of figure \ref{fig:elevationmapdimensions}.

\begin{table}[!htb]
  \caption[Table caption shown in TOC.]{Pioneer 3-AT dimensions.}
  \label{tab:robotDimensions}
  \renewcommand{\arraystretch}{1.2} % more space between rows
  \centering
  \begin{tabular}{ccc}
    \toprule
    $\WheelRadius\,[m]$ & $\WheelLonSeparation\,[m]$ & $\WheelLatSeparation\,[m]$ \\
    \midrule
    0.111 & 0.268 & 0.394 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[!htb]
  \begin{minipage}[b]{0.62\linewidth}
    \centering
    \includegraphics[width=\linewidth]{Figures/elevationMap1.png}
  \end{minipage}
  \hspace{0.3em}
  \begin{minipage}[b]{0.32\linewidth}
    \centering
	 \begin{tabular}{@{}ccccc@{}}
    \toprule
    	\multicolumn{5}{c}{\textbf{Map dimensions}}\\
    \cmidrule{1-5}
    	$\Elevation_{max}$ [m] & $\MapDimx$ [m] & $\MapDimy$ [m] & $\GridDimX$ & $\GridDimY$\\
    \midrule
    	6.58 & 47.0 & 47.0 & 500 & 500\\
    \bottomrule
  \end{tabular}
  \vspace{10em}
  \end{minipage}
\caption{Digital elevation map and respective dimensions.}
\label{fig:elevationmapdimensions}
\end{figure}

The origin of each elevation map is considered to be an inertial world frame $\WorldFrame$, which is placed at the center of each elevation map, as it is depicted in the picture of figure \ref{fig:elevationmapdimensions}. Typically, the original files of elevation maps come as \texttt{.tif} or \texttt{.png} with grayscale encoding. Moreover, the maximum height shall be chosen in such a way that non-negligible crossable slopes are on the map.

Each algorithm is coded in \texttt{Python}, whereas the NLP presented in \refeq{eq:minHeight} is implemented with the casADi numerical optimization tool \cite{Andersson2018}, where IPOPT \cite{wachter2006implementation} is selected as solver. Two different sets of simulations are performed for each map: the first only considers a single contact point at each wheel, whereas the second one takes a set of five hull points at each wheel (figure \ref{fig:hullPoints}).

\begin{figure}[!htb]
	\centering
	\begin{subfigmatrix}{2}
	\subfigure[Single hull point \textit{per} wheel (\shpw)] {
		\begin{tikzpicture}
			\draw (0, -2.0) -- (1.5, -2.0);% bottom line
  			\draw (0, 1) -- (1.5, 1);% top line
  			\draw[dashed] (0,-2.0) arc (-90:90:0.35 and 1.5); % left half of the left ellipse		
  			\draw (0, -2.0) arc (270:90:0.35 and 1.5);	% left half of the left ellipse		
  			\draw (1.5, -0.5) ellipse (0.35 and 1.5);	% right ellipse
  			
  			%	Center of right ellipse
  			\fill[black] (1.5, -0.5) circle (0.03cm);
					
			%	Contact point of 3D wheel
    		\fill[red] (0.75, -2.0) circle (0.06cm);
			
			%	Arrow
			\draw[->,thick] (2.5, -0.5) -- (3.0, -0.5);
			
			%	Right circle			
			\draw[thick, color = black] (5.0, -0.5) circle [radius = 1.5];
			
			%	Center of right circle
			\fill[black] (5.0, -0.5) circle (0.03cm);
			
			%	Contact point
			\fill[red] (5.0, -2.0) circle (0.06cm);
		\end{tikzpicture}
	}
	\subfigure[Several hull points \textit{per} wheel (\mhpw)] {
		\begin{tikzpicture}
  			\draw (0, -2.0) -- (1.5, -2.0);% bottom line
  			\draw (0, 1) -- (1.5, 1);% top line
  			\draw[dashed] (0,-2.0) arc (-90:90:0.35 and 1.5); % left half of the left ellipse		
  			\draw (0, -2.0) arc (270:90:0.35 and 1.5);	% left half of the left ellipse		
  			\draw (1.5, -0.5) ellipse (0.35 and 1.5);	% right ellipse
  			
  			%	Center of right ellipse
  			\fill[black] (1.5, -0.5) circle (0.03cm);
  			
			%\node[red] at (0.24748737341, 0.43933982822 - 2.0) {\scriptsize x};
			%\node[red] at (0.13393920132, 0.11418070123 - 2.0) {\scriptsize x};
			
			\node[red] at (0.24748737341 + 0.75, 0.43933982822 - 2.0) {\scriptsize x};
			\node[red] at (0.13393920132 + 0.75, 0.11418070123 - 2.0) {\scriptsize x};
%			\fill[red]
			
    			\foreach \angle in {-135, -112.5, -90.0} {
    				\fill[red] ({0.35 * cos(\angle)}, {1.5 * sin(\angle)}) + (0.75, -0.5) circle (0.06cm);
    			}
			
			\draw[->,thick] (2.5, -0.5) -- (3.0, -0.5);
			\draw[thick, color = black] (5.0, -0.5) circle [radius = 1.5];
			
			%	Center of right circle
			\fill[black] (5.0, -0.5) circle (0.03cm);
			
			\foreach \angle in {-135, -112.5, -90.0, -67.5, -45} {
				\fill[red] ({5.0 + cos(\angle) * 1.5}, {-0.5 + sin(\angle) * 1.5}) circle (0.06cm);
			}
		\end{tikzpicture}
	}
	\end{subfigmatrix}
	\caption{Hull points on each wheel}
    \label{fig:hullPoints}
\end{figure}

Apart from the restriction that every hull point must be above or at contact with the elevation map surface, linear, symmetric euler angle restrictions are added (see table \ref{tab:eulerAnglesRestrictions}).

\begin{wraptable}{r}{0.4\textwidth}
  \caption[Table caption shown in TOC.]{Euler angles restrictions.}
  \label{tab:eulerAnglesRestrictions}
  \renewcommand{\arraystretch}{0.8} % more space between rows
  \centering
  \begin{tabular}{cc}
    \toprule
    $\roll_{max}$ [rad] & $\pitch_{max}$ [rad] \\
    \midrule
    $\frac{\pi}{4}$ & $\frac{\pi}{4}$ \\
    \bottomrule
  \end{tabular}
\end{wraptable}

Below, the computed traversability maps are presented for \shpw, under the conditions presented in this subsection. Traversability maps that match the results ranging from algorithm \ref{alg:slopeMinHeightMapVarYaw} to \ref{alg:refinedMap} are presented, in order to enhance the differences among all maps.

\begin{figure}[!ht]
    \centering
	\begin{subfigmatrix}{4}
	\subfigure[Algorithm \nameref{alg:slopeMinHeightMapVarYaw}] {
    	\includegraphics[width=0.48\linewidth]{Figures/HeightMin_maxInclination+Points+1.png}  
	}
	\subfigure[Algorithm \nameref{alg:maxRoughnessMap}] {
    	\includegraphics[width=0.48\linewidth]{Figures/maxTerrainRoughness+1.png}
	}
	\subfigure[Worst-case map] {
    	\includegraphics[width=0.48\linewidth]{Figures/worstCase+Points+1.png}  
	}
	\subfigure[Algorithm \nameref{alg:refinedMap}, where $r_s$ = 1 m and $C$ = 0.9] {
    	\includegraphics[width=0.48\linewidth]{Figures/mapRefinement+Points+0.9+1.0+1.png}  
	}
	\end{subfigmatrix}
    \caption{Traversability maps generated with different algorithms (\acrshort{shpw}).}
    \label{fig:costMapsCase1Map1}
\end{figure} 

The traversability maps which were obtained for the \shpwgap variant are rendered in figure \ref{fig:costMapsCase1Map1}. For the sake of brevity, the maps computed for \mhpwgap are presented in annex \ref{section:traversabilityMaps}, as they are graphically similar to the maps presented in figure \ref{fig:costMapsCase1Map1}. The maximum terrain roughness map is the same both for \shpwgap and \mhpw.

\begin{remark}
A suitable rescaling of the elevation map shall be made with the intention of yielding a large enough map, such that its discretization is of the same magnitude order of the wheel diameter.
\end{remark}

\subsection{Discussion}
\label{subsection:costMapsDiscussion}

Algorithms \ref{alg:slopeMinHeightMap} to \ref{alg:refinedMap} were built in such a way that the output map assigns higher costs to hilly or irregular terrain. On the other hand, it was required that the output map captures the terrain height gradient, although it is also advisable to assign higher costs to positions which are close to untraversable areas, to discourage traversals there. Therefore, a balance between a traversability map that leads to the computation of a safe path and an accurate capture of the terrain morphology is sought.

The analysis of figure \ref{fig:costMapsCase1Map1} and tables \ref{tab:resultsMap_1_Case1_2_Alg2_3} and \ref{tab:results_Map1_Case1_2_Alg_4_5} express the fact that the generated maps capture well the terrain morphology, as the correlation between the height gradient and the cost is very high on all cases. However, that correlation decreases a lot when algorithm \nameref{alg:refinedMap} is applied, due to the fact that the map refinement assigns higher costs to positions which are close to untraversable positions, irrespectively of the terrain inclination there. This is the price to pay in case one wants to increase the likelihood of avoiding hazardous positions. The correlation between $\nabla \FunctionElevation(\cdot)$ and $\FunctionTraversability(\cdot)$ also depends on parameters $r_s$ and $C$. The higher $r_s$ and the lower $C$ are, the source that radiates from each point with cost higher or equal to $C$ is applied to a larger set of points, which causes this correlation to be weaker.

\begin{table}[!htb]
  \caption{Comparison between algorithms \nameref{alg:slopeMinHeightMap} and \nameref{alg:slopeMinHeightMapVarYaw}}
  \label{tab:resultsMap_1_Case1_2_Alg2_3}
  \centering
  \renewcommand{\arraystretch}{1.2}
  \begin{tabular}{@{}rrrcrr@{}}
    \toprule
      & \multicolumn{2}{c}{\shpw} & \phantom{a} & \multicolumn{2}{c}{\mhpw} \\
    \cmidrule{2-3}
    \cmidrule{5-6}
    $\text{Algorithm}$ & \nameref{alg:slopeMinHeightMap} & \nameref{alg:slopeMinHeightMapVarYaw} && \nameref{alg:slopeMinHeightMap} & \nameref{alg:slopeMinHeightMapVarYaw}\\
    \midrule
    $\text{[ms] / cell}$ & $(5.82)\pm(1.58)$ & $(71.11)\pm(9.96)$ && $(10.10)\pm(3.00)$ & $(110.33)\pm(13.68)$\\
    $\text{iterations / cell}$ & $(8.99)\pm(2.13)$ & $(118.17)\pm(12.36)$ && $(11.71)\pm(2.79)$ & $(151.36)\pm(14.45)$\\
    $\Barrier^*\,[\times10\mathrm{e}{-9}]$ & $(2.46)\pm(2.67\mathrm{e}{-1})$ & $(2.46)\pm(2.70\mathrm{e}{-1})$ && $(2.50)\pm(7.86\mathrm{e}{-2})$ & $(2.50)\pm(7.72\mathrm{e}{-2})$ \\
    $\nabla \FunctionElevation(\cdot) \text{ vs }\FunctionTraversability(\cdot) \text{ [R]}$ & $0.89$ & $0.83$ && $0.90$ & $0.93$\\
    \bottomrule
    \multicolumn{6}{c}{}
  \end{tabular}
\end{table}

%\begin{table}[!htb]
%  \caption{Comparison between algorithms \nameref{alg:maxRoughnessMap} and \nameref{alg:refinedMap}}
%  \label{tab:results_Map1_Case1_2_Alg_4_5}
%  \centering
%  \renewcommand{\arraystretch}{1.2}
%  \begin{tabular}{@{}rrrrcrr@{}}
%    \toprule
%     & & \multicolumn{2}{c}{Case 1} & \phantom{abc} & \multicolumn{2}{c}{Case 2}\\
%    \cmidrule{3-4}
%    \cmidrule{6-7}
%    $\text{Algorithm}$ & \nameref{alg:maxRoughnessMap} & Worst-case & \nameref{alg:refinedMap} && Worst-case & \nameref{alg:refinedMap} \\
%    \midrule
%    $\bar{t} \text{ / cell [ms]}$ & 8.51 $\pm$ 1.56 & 3.12e-6 $\pm$ 8.10e-4 & $\text{1.40}^*$ && 3.12e-6 $\pm$ 7.63e-4 & $\text{1.49}^*$ \\
%    $\nabla \FunctionElevation(\cdot) \text{ vs }\FunctionTraversability(\cdot) \text{ [R]}$ & 0.96 & 0.78 & 0.61 && 0.50 & 0.13 \\
%    \bottomrule
%    \multicolumn{6}{c}{$\dagger$ {\footnotesize \text{Worst-case map}}  $\ast$ {\tiny $\frac{ \text{Total computation time} }{ \text{Number of cells} }$}  $\ast\ast$ {\footnotesize $r_s = 1$ m}, $C  = 0.9$ ; {\tiny (mean) $\pm$ (standard deviation), with confidence interval of 68.27 \%, when applicable}}
%  \end{tabular}
%\end{table}

\begin{table}[!htb]
  \caption{Performance of algorithms \nameref{alg:maxRoughnessMap} and \nameref{alg:refinedMap}}
  \label{tab:results_Map1_Case1_2_Alg_4_5}
  \centering
  \renewcommand{\arraystretch}{1.2}
  \begin{tabular}{@{}rrrcr@{}}
    \toprule
     & & \multicolumn{1}{c}{\shpwgap} & \phantom{abc} & \multicolumn{1}{c}{\mhpwgap}\\
    \cmidrule{3-3}
    \cmidrule{5-5}
    $\text{Algorithm}$ & \nameref{alg:maxRoughnessMap} & $\text{\nameref{alg:refinedMap}}^{**}$ && $\text{\nameref{alg:refinedMap}}^{**}$ \\
    \midrule
    $\bar{t} \text{ / cell [ms]}$ & $(8.51)\,\pm\,(1.56)$ & $1.40^*$ && $1.49^*$ \\
    $\nabla \FunctionElevation(\cdot) \text{ vs }\FunctionTraversability(\cdot) \text{ [R]}$ & $0.96$ & $0.61$ && $0.13$\\
    \bottomrule
    \multicolumn{5}{c}{$\ast$ {\tiny $\frac{ \text{Total computation time} }{ \text{Number of cells} }$}  $\ast\ast$ {\footnotesize $r_s = 1$ m}, $C  = 0.9$ ; {\tiny (mean) $\pm$ (standard deviation), with confidence interval of 68.27 \%, when applicable}}
  \end{tabular}
\end{table}

As it is expected, the computational burden increases a lot, when algorithms \nameref{alg:slopeMinHeightMap} and \nameref{alg:slopeMinHeightMapVarYaw} are run for \mhpw, because the NLP needs to account for more constraints. However, the computation time is not the most relevant result because these algorithms are intented to be run offline, prior to any traversal. 

Optimality is assessed by evaluating the optimal barrier parameter $\Barrier^*$ \cite{diehlnumericalocp}. As $\Barrier^*$ is reported to be very small on all cases, the computed costs relate to solutions of a very close version of the NLP presented in \refeq{eq:minHeight}. The IPOPT solver consecutively decreases $\Barrier$ along its iterations by consecutively solving closer NLPs to the original one. In the case of algorithm \nameref{alg:slopeMinHeightMapVarYaw}, the number of iterations per cell were considered to be the total number of iterations performed across all optimizations at every yaw in $\mathcal{Y}$.

Finally, it is worth to note that the traversability map computed by algorithm \nameref{alg:slopeMinHeightMapVarYaw} for \mhpwgap (figure \ref{fig:costMapsCase2Alg2_4}) finds more untraversable positions in comparison to variant \shpwgap (figure \ref{fig:costMapsCase1Map1}), because it is more difficult to accomodate more hull points on the terrain, thus placing the vehicle on intricate poses.

\section{Optimal path planning}
\label{section:optimalPathPlanning}

From now on, it is possible to compute a potential flow with respect to any cost map presented in section \ref{section:costMap}. The solution of the Eikonal equation outputs a function $\timeFunction\cdot$ that holds the time it takes to reach $G$ from any position $\mathbf{p}_2 \in \Map_2$. At the end, $\nabla \timeFunction\cdot$ provides the optimal path direction towards the selected goal at $\mathbf{p}_2 \in \Map_2$. As $\timeFunction\cdot$ does not possess local minimums, there is no risk that the computation of the optimal path gets stuck.

\begin{figure}
\begin{minipage}[c]{0.48\linewidth}
	\centering
	\begin{tikzpicture}[domain=-0.2:2.2]
  		\draw[->] (-0.2, 0.0) -- (2.2, 0.0) node[right] {$\FunctionTraversability_d \left( x_i, \,y_j \right)$};
  		\draw[->] (0.0, -0.2) -- (0.0, 1.5) node[above] {$\speedFunction{x_i, \,y_j}$};
  		\draw[color=red] plot (\x, {exp(-2*\x)}) node[above=20pt, right=10pt] {equation \eqref{eq:cost2SpeedConversion}};
  		\draw[dotted, thick] (0.6, 0.0) -- (0.6, 0.30119421191);  % Vertical dotted line
  		\draw[dotted, thick] (0.6, 0.30119421191) -- (0.0, 0.30119421191); % Horizontal dotted line
  		% Mark the intersection point
  		\fill (0.6, 0.30119421191) circle (1pt); % Small dot at the intersection
  		\draw (0.6, 0.0) node[below] at (0.6, 0.0) {$k_2$};
  		\draw (0.0, 0.30119421191) node[left] at (0.0, 0.30119421191) {$k_1$};
  		\draw (0.0, 1.0) node[left] at (0.0, 1.0) {$1$};
  		\draw (1.0, 0.0) node[below] at (1.0, 0.0) {$1$};
  		\draw (1.0, 0.0) -- (1.0, -0.05);
	\end{tikzpicture}
\end{minipage}
\hfill
\begin{minipage}[c]{0.48\linewidth}
	\begin{equation}
		\speedFunction{x_i, y_j} = e^{\frac{\ln(k_1)}{k_2} \cdot \FunctionTraversability_d(x_i, y_j)}
		\label{eq:cost2SpeedConversion}
	\end{equation}
\end{minipage}
\captionof{figure}{Cost to speed transformation, $\FunctionTraversability_d(\cdot) \longmapsto  \speedFunction{\cdot}$, with \refeq{eq:cost2SpeedConversion}.}
	\label{fig:cost2SpeedFunction}
\end{figure}

In order to solve the Eikonal equation, $\speedFunction\cdot$ shall be replaced by a speed map. With the intention of achieving it,  the selected cost map must be transformed into a speed map, because lower speed maps must match higher costs.

Although this transformation can take many forms, it must comply with the fact that higher costs match lower speeds, an exponential function of the type $e^{-kx}$ is chosen in this thesis. Equation \eqref{eq:cost2SpeedConversion} is a particular case of $e^{-kx}$ where $-k = \frac{\ln\left(k_1\right)}{k_2}$ in order to enforce that if $\FunctionTraversability_d \left(x_i,\, y_j\right) \geq k_2$, it comes that $\speedFunction{x_i,\,y_j} \leq k_1$. The choice of $k_1$ and $k_2$ is made in such a way, so that a very high cost from $\FunctionTraversability_d \left(\cdot\right)$ is transformed into a very small speed in $\speedFunction{\cdot}$.

Each value that is assigned to every cell of the speed map expresses the fact that it is safer or riskier to pass by that position. If $\speedFunction\cdot$ presents a high (low) value to some cell of the speed map, that means it is safer (riskier) to pass by that position. Unlike occupancy maps, speed maps can take any non-negative value at each cell. On both maps, if zero is assigned to some position, that position is \ti{seen} as an obstacle. However, one may enlarge the set of obstacles in $\Map_2$ by \ti{masking} (see subsection \ref{subsection:implementationfm}) the cells of $\speedFunction{\cdot}$ that match the condition $\FunctionTraversability_d\left(\cdot\right) \geq c_{max}$, where $c_{max}$ denotes the maximum admissable cost for traversal.

\subsection{Implementation}
\label{subsection:implementationfm}

\begin{wrapfigure}{r}{0.45\textwidth}
  \begin{center}
  	\includegraphics[width=0.4\textwidth]{Figures/optimal_path_generation.pdf}
  \end{center}
  \caption{\nameref{alg:optimalPathGeneration} development concept: the next cell is the minimum time cell among the cells surrounding the previous one.}
  \label{fig:optimalpathgenconcept}
\end{wrapfigure}

Algorithm \nameref{alg:speedMapGeneration} details the development of the speed map generation, whereas algorithm \nameref{alg:optimalPathGeneration} encapsulates the optimal path generation method described in the previous section, where $N$ denotes the path length. Algorithm \nameref{alg:speedMapGeneration} is meant to be run offline, but algorithm \nameref{alg:optimalPathGeneration} is not, so the latter was implemented in \texttt{C} to speed up its execution and allow for greater time margins in the iteration window, within a real-time simulation setting. Then, function \textsc{getoptimalpath($\cdot$)} is called in \texttt{Python} using the well-known \texttt{ctypes} package.

The computation of a discrete solution $\speedFunction\cdot$ for the Eikonal equation is made by applying the method \texttt{travel\_time()} from the \texttt{scikit-fmm} Python package\footnote{scikit-fmm package: \texttt{\url{https://github.com/scikit-fmm/scikit-fmm}} (accessed on 23 of March 2026)}. Finally, the optimal path is obtained by repeatedly finding the minimum time position around the newest path position, until the goal position $G$ is reached or if the path surpasses its neighborhood, $\neighborhood(G)$.

\begin{algorithm}[H]
	\caption{\footnotesize \textsc{POTENTIALFLOW}}
	\label{alg:speedMapGeneration}
	\centering
	\footnotesize
	\begin{algorithmic}[1]
	\Function{getpotentialflow}{$\FunctionTraversability_d \left( \cdot \right), \,n_x, \,n_y, \, k_1,\, k_2, \,c_{max}$}
	\ForEach{$\{i,\,j\} \in \{0,\, \ldots, \, n_x - 1\} \times \{0,\, \ldots, \, n_y - 1\}$}
		\If{$\FunctionTraversability_d \left( x_i,\,y_j \right) \geq c_{max}$}
			\State $\speedFunction{x_i,\,y_j} \gets 0$ \Comment{Label this position as an obstacle (masking).}
		\Else
			\State $\speedFunction{x_i,\,y_j} \gets \text{equation \eqref{eq:cost2SpeedConversion}}$ \Comment{Turn cost into speed.}
		\EndIf
	\EndFor
	\State $\timeFunction{\cdot} \gets \text{equation \eqref{eq:eikonalEquation}}$ \Comment{Solve eikonal equation}
	\State \Return $\timeFunction{\cdot}$
	\EndFunction
	\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
	\caption{\footnotesize \textsc{OPTIMALPATH}}
	\label{alg:optimalPathGeneration}
	\centering
	\footnotesize
	\begin{algorithmic}[1]
	\Function{getoptimalpath}{$\timeFunction{\cdot}, \,S, \,G, \,N$}
	\If{$S \in \ObstacleSpace \; \text{or} \; G \in \ObstacleSpace$}
		\State \Return $\text{empty path}$ \Comment{Reject path generation in case $S$ or $G$ positions are labeled as obstacles.}
	\EndIf
	\State $\left( x_i,\,y_j \right) \gets S,\, \text{index} \gets 0$ \Comment{Generate optimal path from starting position $S$}
	\While{$\left( x_i,\,y_j \right) \notin \neighborhood(G) \text{ and index} \leq N$}
		\State $\left( x_i,\,y_j \right) \gets \textsc{getposition} \left( \textsc{min} \left( \timeFunction{ \neighborhood\left( x_i,\,y_j \right)}\right)\right)$ \Comment{Get minimum time position among contiguous cells.} 
		\State $\text{path.append}\left( x_i,\,y_j \right)$ \Comment{Append minimum time position to path.}
		\State $\text{index} \gets \text{index + 1}$
	\EndWhile
	\While{$\text{index} \leq N$}
		\State $\text{path.append}\left( x_i,\,y_j \right)$ \Comment{Fill path with last position until $N+1$ positions are gathered.}
		\State $\text{index} \gets \text{index + 1}$
	\EndWhile
	\State \Return $\text{path}$
	\EndFunction
	\end{algorithmic}
\end{algorithm}

\subsection{Discussion}
\label{subsection:discussionfm}

In this subsection, the results generated by algorithms \nameref{alg:optimalPathGeneration} and \nameref{alg:speedMapGeneration} are presented and discussed.

Figure \ref{fig:speedMapPotentialWorstCasePointsMap1} renders a speed map that was generated with respect to a worst-case traversability map for case \shpwgap and the elevation map of figure \ref{fig:elevationmapdimensions}, by applying transformation \eqref{eq:cost2SpeedConversion}. Then, a potential flow is generated by executing algorithm \nameref{alg:speedMapGeneration}, which yields a solution $\timeFunction{\cdot}$ of the Eikonal \refeq{eq:eikonalEquation}. There, $-\nabla \timeFunction{\cdot}$ is rendered all over the map on blue arrows, and it is perpendicular to every isochrone line (black lines), as expected. Instead of applying algorithm \nameref{alg:optimalPathGeneration}, optimal paths may also be generated by following $-\nabla \timeFunction{\cdot}$ until the goal point (or its neighborhood) is reached \cite{fmAdvancedGomez}.

Figure \ref{fig:potentialFlowWithPathsPointsMap1} presents several optimal paths generated with algorithm \nameref{alg:optimalPathGeneration}. All these paths meet at the goal point, and follow a gradient descent direction as expected. It is also verified that the computed paths successfully avoid the masked area (white area). The refinement of the worst-case map does not considerably change the respective potential flow, even if the masked area is greater. This is why the optimal paths presented in figure \ref{fig:potentialFlowWithPathsPointsMap1} are similar, provided that the starting and goal points are the same. However, this similarity is not a trend. In fact, provided that parameters $r_s$ and $C$ yield a great change from the worst-case to the refined traversability maps, the optimal paths generated with respect to both maps turns out to be very different.

\begin{figure}[!ht]
    \centering
	\begin{subfigmatrix}{2}
	\subfigure[Unmasked speed map $\speedFunction{\cdot}$, where $k_1 = 0.2,\; k_2 = 0.4$.] {
    	\includegraphics[width=0.48\linewidth]{Figures/speedMap+worstCase+Points.png}  
	}
	\subfigure[Masked potential flow $\timeFunction{\cdot}$ and $-\nabla \timeFunction{\cdot}$ (\textcolor{blue}{blue arrows}), where $c_{max}=0.6$. Masked area is on white.] {
    	\includegraphics[width=0.48\linewidth]{Figures/potentialFlow+worstCase+Points.pdf}  
	}
	\end{subfigmatrix}
    \caption{Results of algorithm \nameref{alg:speedMapGeneration} with respect to worst-case traversability map (\shpw).}
    \label{fig:speedMapPotentialWorstCasePointsMap1}
\end{figure}

\begin{figure}[!ht]
    \centering
	\begin{subfigmatrix}{2}
	\subfigure[Worst-case map $\left(c_{max} = 0.6\right)$.] {
    	\includegraphics[width=0.48\linewidth]{Figures/potentialFlow+paths+worstCase+Points.pdf}  
	}
	\subfigure[Refined map $\left(r_s = 1.0\,\mathrm{m},\, C = 0.9,\,c_{max} = 0.6\right)$.] {
    	\includegraphics[width=0.48\linewidth]{Figures/potentialFlow+paths+mapRefinement+Points.pdf}  
	}
	\end{subfigmatrix}
    \caption{Results of algorithm \nameref{alg:optimalPathGeneration} (\shpw). Masked area is on white.}
    \label{fig:potentialFlowWithPathsPointsMap1}
\end{figure}

\begin{remark}
If the \nameref{alg:speedMapGeneration} algorithm is fed by a worst-case map, it is advisable to select $c_{max}$ in such a way that $c_{max}$ is below the true admissable cost, in order to establish a safety margin. In case \nameref{alg:speedMapGeneration} is fed by a refined map, the safety margin is already taken into account on the refined map generation, so $c_{max}$ may be equal to the true admissable cost.
\end{remark}

Once more, figure \ref{fig:pathsComparison+_getPat2FollowTimes} underlines the fact that  different cost maps typically yield different speed maps, which in turn yield different potential flows. In the end, different optimal paths are obtained. The table next to figure \ref{fig:pathsComparison+_getPat2FollowTimes} presents data related to the runtimes of the implementation of function \textsc{getoptimalpath($\cdot$)} from algorithm \nameref{alg:optimalPathGeneration} in \texttt{C}. This function was run 27083 times for different maps and starting positions. The map refinement parameters, equation \eqref{eq:cost2SpeedConversion} parameters and goal point remained fixed. In case  $S \in \ObstacleSpace$, that runtime was discarded.

\begin{figure}[!htb]
  \begin{minipage}[b]{0.62\linewidth}
    \centering
    \includegraphics[width=\linewidth]{Figures/comparisonOfPaths.pdf}
  \end{minipage}%
  \begin{minipage}[b]{0.32\linewidth}
    \centering
    \begin{tabular}[b]{ll}
      \toprule
        \multicolumn{2}{c}{27083 executions} \\
      \midrule
        avg$\left(t\right)$ [ms] & 2.671\\
        std$\left(t\right)$ [ms] & 0.204\\
        max$\left(t\right)$ [ms] & 17.339\\
        min$\left(t\right)$ [ms] & 2.282\\
      \bottomrule
    \end{tabular}
    \vspace{5em}
  \end{minipage}
\caption{Paths comparison and \textsc{getoptimalpath($\cdot$)} method runtimes.}
\label{fig:pathsComparison+_getPat2FollowTimes}
\end{figure}

\begin{table}[!htb]
  \caption{Performance metrics of the optimal paths presented in Figure \ref{fig:pathsComparison+_getPat2FollowTimes}. The refinement maps taken for this computation are presented in figure \ref{fig:costMapsCase2Alg5}.}
  \label{tab:performanceMetricOptPathsDiffMaps}
  \renewcommand{\arraystretch}{1.2}
  \centering
  \begin{tabular}[]{lcccc}
	\toprule
	& \shortstack{WorstCase+\\\shpw} & \shortstack{WorstCase+\\\mhpw} & \shortstack{MapRefinement+\\\shpw} & \shortstack{MapRefinement+\\\mhpw}\\
	\midrule
	Total cost & 48.80 & 53.05 & 48.60 & 73.76\\
	Distance traversed [m] & 51.89 & 54.06 & 51.89 & 67.79\\
  	\bottomrule
  \end{tabular}
\end{table}

The results show that function \textsc{getoptimalpath($\cdot$)} is feasible for implementation within a system control loop, where time windows are typically around 100 ms.

Apart from that, table \ref{tab:performanceMetricOptPathsDiffMaps} presents the sum of the costs of every cell of the optimal path, so this value expresses how much unevenness a vehicle may find in case it tracks this path. Then, the length of each path is also presented. At the end, one realises that the optimal path generated with respect to the MapRefinement+\shpwgap variant yielded the shorter and the least uneven traversal. It was expected that the refined map could output better results, as this map promotes the avoidance from hilly terrain, even though results generated with respect to the \mhpwgap variant should have been better, as it models with higher precision the hull of the robot. Although there is room for the improvement of NLP \ref{eq:minHeight} in case of variant \mhpw, the fact that more hull points are attached to the robot hull, gives less freedom to the NLP to find lower inclinations. Therefore, one may also look to the \mhpwgap variant as a more conservative solution, because the unevenness of the terrain that was captured is more in line with the true vehicle hull capability to adapt to that terrain.

\begin{remark}
Due to the fact that \textsc{getoptimalpath($\cdot$)} returns \texttt{None} in case $S \in \ObstacleSpace$, one should be aware that $\ObstacleSpace$ is not scattered along the map, but rather is restricted to \ti{blocks} of obstacles, in order to prevent frequent rejections of an optimal path computation.
\end{remark}